# public/js/

Purpose
- Client-side JavaScript for UI behaviors, AJAX calls for booking or status change, form validation, and interactivity.

Notes
- For production, consider bundling/minifying these scripts.
- Keep dependency-free code or vendor libraries separate (e.g., `vendor/` or use CDN links in templates).
