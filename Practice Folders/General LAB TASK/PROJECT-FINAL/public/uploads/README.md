# public/uploads/

Purpose
- Stores uploaded assets such as doctor profile photos and other media created at runtime.

Important
- Do not commit uploaded files to source control. Add `public/uploads` to `.gitignore`.
- Seeded images used by `seed.js` are placed here so seeded doctors have images to display.
