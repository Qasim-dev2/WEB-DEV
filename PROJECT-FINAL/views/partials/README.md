# views/partials/

Purpose
- Shared EJS partials included across multiple pages (header, footer, navigation, flash messages).

Typical partials
- `header.ejs`, `footer.ejs`, `nav.ejs`, `messages.ejs`.

Notes
- Keep markup and small UI logic here; avoid controller logic in partials.
